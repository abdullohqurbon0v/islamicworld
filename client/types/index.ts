export interface ChildProps {
  children: React.ReactNode;
}

export interface UserType {
    id: string,
    createdAt: Date
    email: string
    fullName: string
    phone: string
    avatar: string
}
